name: giso_tracker
description: GISO tracking SDK for Flutter — page/biz events, gzip batch upload, registry constants.
version: 1.0.7
homepage: https://github.com/cloud-gido/giso
repository: https://github.com/cloud-gido/giso
issue_tracker: https://github.com/cloud-gido/giso/issues

publish_to: none

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^8.3.0
  connectivity_plus: ^6.1.4
  device_info_plus: ^11.3.0
  http: ^1.2.2
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

platforms:
  android:
  ios:
