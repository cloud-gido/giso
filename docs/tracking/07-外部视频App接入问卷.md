# 外部视频 App · GISO 埋点接入问卷

> 请 **产品 + 客户端开发** 共同填写，回传给 GISO 数据负责人。  
> 用途：判断哪些埋点可复用现有登记、哪些需要新增 `schema`，以及分配 App Key 与联调计划。  
> 填写说明：在 `[ ]` 打勾，在 `____` 填空；表格可复制行增删。

---

## 一、项目基本信息

| 项 | 填写 |
|----|------|
| 业务 / App 名称 | |
| 平台 | [ ] Android  [ ] iOS  [ ] 双端  [ ] 含 H5 |
| 包名 / Bundle ID | |
| 预计内测时间 | |
| 预计正式上线 | |
| 产品负责人 | 姓名 / 联系方式 |
| 客户端负责人 | 姓名 / 联系方式 |
| 希望使用的 App Key 名称（可我们命名） | 内测：`________-android-beta`  正式：`________-android-prod` |

**接入环境（测试阶段由我方提供）：**

| 项 | 值 |
|----|-----|
| 上报地址 | `https://gamelinelab-giso.envir.dev/v1/track` |
| 内测 env | `test`（SDK `debug=true`） |
| 正式 env | `prod`（SDK `debug=false`） |

---

## 二、产品形态（帮我们判断能否复用 Demo）

请描述 App 主流程（可附原型图 / 截图链接）：

```
例：开屏 → 推荐流 → 点卡片 → 长视频播放页 → 选集 → 分享
```

| # | 问题 | 回答 |
|---|------|------|
| 1 | 视频类型 | [ ] 长视频剧集  [ ] 短视频流  [ ] 直播  [ ] 混合  [ ] 其他：____ |
| 2 | 是否有「推荐流 / 首页 feed」 | [ ] 有  [ ] 无 |
| 3 | 是否有独立「播放页」 | [ ] 有  [ ] 无（信息流内播放） |
| 4 | 是否有「剧集 / 选集」 | [ ] 有  [ ] 无 |
| 5 | 是否有广告（前贴 / 中插） | [ ] 有  [ ] 无 |
| 6 | 是否有打赏 / 礼物 | [ ] 有  [ ] 无 |
| 7 | 是否需要登录态分析 | [ ] 是（提供 uid）  [ ] 否 |
| 8 | 与「玑源长视频 Demo」流程是否基本一致 | [ ] 是，可复用 Demo  [ ] 部分一致  [ ] 完全不同 |

---

## 三、页面清单（page_enter / page_exit）

> 每个主要界面填一行。若与下表「已有 pgid」一致，在「是否复用」打勾即可。

### 3.1 我方已有、长视频常用页面（供对照）

| 已有 pgid | 含义 |
|-----------|------|
| `video_feed` | 视频推荐流 / 首页 Tab |
| `video_detail` | 长视频播放详情页 |
| `video_series` | 剧集合集 / 选集页 |

### 3.2 贵方页面（请填写）

| # | 界面名称（中文） | 建议 pgid（可留空由我方命名） | 是否复用已有 pgid | 进入时必带参数（如 vid、series_id） | 备注 |
|---|------------------|-------------------------------|-------------------|--------------------------------------|------|
| 1 | | | [ ] `video_feed`  [ ] 新建 | | |
| 2 | | | [ ] `video_detail`  [ ] 新建 | | |
| 3 | | | [ ] `video_series`  [ ] 新建 | | |
| 4 | | | [ ] 新建：________ | | |
| 5 | | | [ ] 新建：________ | | |

---

## 四、元素清单（曝光 / 点击）

> 按钮、卡片等可点击/可曝光控件。元素 **全局唯一**，不写进页面名（✅ `video_card` ❌ `feed_video_card`）。

### 4.1 我方已有、视频常用元素（供对照）

| 已有 eid | 含义 |
|----------|------|
| `video_card` | 视频卡片（封面+标题等） |
| `play_btn` | 播放按钮 |
| `like_btn` | 点赞 |
| `share_btn` | 分享 |
| `episode_item` | 选集条目 |
| `fullscreen_btn` | 全屏 |
| `bottom_tab` | 底部 Tab |

### 4.2 贵方元素（请填写）

| # | 元素名称 | 出现在哪些页面 | 是否复用已有 eid | 点击/曝光都要？ | 必带参数 | 备注 |
|---|----------|----------------|------------------|-----------------|----------|------|
| 1 | | | [ ] `video_card`  [ ] 新建 | [ ] 曝光+点击 | vid? | |
| 2 | | | [ ] `play_btn`  [ ] 新建 | | | |
| 3 | | | [ ] 新建：________ | | | |
| 4 | | | [ ] 新建：________ | | | |

---

## 五、播放与业务事件（biz_event）

> 播放时长、完播等用 **业务事件**，不用自造 `xxx_click` 事件名。

### 5.1 我方已有、播放链路事件（供对照）

| 已有 code | 含义 | 主要参数 |
|-----------|------|----------|
| `video_play_start` | 开始播放 | vid, series_id, ep_num, is_auto, definition |
| `video_play_heartbeat` | 播放心跳（可选） | vid, play_pos |
| `video_play_end` | 结束播放 | vid, play_dur, play_pos, video_dur |
| `video_play_error` | 播放失败 | vid, error_code |

### 5.2 贵方需要的业务指标（请填写）

| # | 要看什么指标 | 是否复用已有 code | 若新建，建议命名 | 需要哪些参数 | 触发时机 |
|---|--------------|-------------------|------------------|--------------|----------|
| 1 | 开始播放 | [ ] `video_play_start`  [ ] 新建 | | vid, … | 首帧 / 用户点播放 |
| 2 | 播放进度/心跳 | [ ] `video_play_heartbeat`  [ ] 不需要  [ ] 新建 | | | 每 N 秒 |
| 3 | 完播 / 退出 | [ ] `video_play_end`  [ ] 新建 | | play_dur, play_pct? | 离开播放页 |
| 4 | 播放失败 | [ ] `video_play_error`  [ ] 新建 | | | |
| 5 | 跳过广告 | [ ] 无  [ ] 需新建 | 例：`video_ad_skip` | ad_id? | |
| 6 | 其他：____ | | | | |

---

## 六、参数清单

> 参数 key 全局登记，**snake_case**，金额用最小单位整数 + currency。

### 6.1 我方已有、视频常用参数（供对照）

| 已有 key | 含义 |
|----------|------|
| `vid` | 视频 ID |
| `series_id` | 剧集 ID |
| `ep_num` | 集数（1-based） |
| `cp_id` | 创作者 ID |
| `play_dur` | 播放时长（毫秒） |
| `play_pos` | 播放进度（秒） |
| `video_dur` | 视频总时长（秒） |
| `is_auto` | 是否自动播放 0/1 |
| `definition` | 清晰度 hd/sd/… |
| `tab_name` | Tab 英文名 |

### 6.2 贵方是否需要额外参数

| # | 参数含义 | 建议 key（可留空） | 类型 string/int | 是否已有可复用 | 取值规则说明 |
|---|----------|-------------------|-----------------|----------------|--------------|
| 1 | | | | [ ] 复用 `vid` 等  [ ] 新建 | |
| 2 | | | | | |
| 3 | | | | | |

**禁止上报：** 手机号、身份证、银行卡号等 PII；用户标识只用 `uid` / `did`（SDK 自动处理）。

---

## 七、与 Demo 差异说明（重要）

若第 2 节选了「部分一致」或「完全不同」，请具体说明：

```
例：
- 我们有竖屏短视频流 page，Demo 没有 → 需新建 pgid video_shorts_feed
- 我们有打赏按钮，Demo 没有 → 需新建 eid gift_btn + biz_event video_gift_send
- 播放页多一个「清晰度切换」按钮 → 可复用 share_btn 类型，新建 eid quality_btn
```

| 差异点 | 处理建议（我方填写） |
|--------|----------------------|
| | |
| | |

---

## 八、技术接入确认（客户端填写）

| # | 项 | 确认 |
|---|-----|------|
| 1 | 使用 GISO Android/iOS SDK（推荐） | [ ] 是  [ ] 暂用 HTTP，计划 ____ 前改 SDK |
| 2 | 内测包 `debug=true`，`env=test` | [ ] 已知晓 |
| 3 | 正式包换 prod App Key | [ ] 已知晓 |
| 4 | 常量使用 `Pages` / `Elements` / `BizEvents`，不手写字符串 | [ ] 已知晓 |
| 5 | 曝光口径由 SDK 统一（不自算曝光） | [ ] 已知晓 |
| 6 | 资金/订单类事实由服务端上报，客户端只报意图 | [ ] 已知晓 / [ ] 不涉及 |

**计划联调时间：** ____  
**联调负责人 did（联调时提供）：** 设备打开 App 后底部浮层或日志中的 did

---

## 九、验收标准（双方对齐）

| # | 验收项 | 标准 |
|---|--------|------|
| 1 | 网关鉴权 | 内测 Key 上报返回 204，错误 Key 返回 401 |
| 2 | 管理台实时联调 | 核心路径事件为 **绿色「正常」**，无大量红色「错误」 |
| 3 | 数据环境 | 内测数据 `env=test`，进入 `giso_events_raw_test` |
| 4 | 核心漏斗（示例，可改） | `page_enter(video_feed)` → `element_click(video_card)` → `page_enter(video_detail)` → `biz_event(video_play_start)` |
| 5 | Doris 可查（可选） | 按 did + `env=test` 能查到上述事件 |

---

## 十、回传与处理时效（我方内部）

| 步骤 | 负责人 | 产出 | 时效参考 |
|------|--------|------|----------|
| 1 | 收到本问卷 | 评审能否复用 Demo | 1 工作日 |
| 2 | 数据负责人 | `schema` 变更 PR（如需） | 1～3 工作日 |
| 3 | 运维 | Doppler 增加 App Key | 0.5 工作日 |
| 4 | 平台 | Gateway 镜像更新（若改 schema） | CI 自动 |
| 5 | 我方 | 下发 SDK + Key + 对接说明（[13-SDK分发与版本](13-SDK分发与版本.md) 清单） | 与上同步 |
| 6 | 对方 | 开发埋点 + 联调 | 按项目 |
| 7 | 双方 | 管理台验收 + 签字确认 | |

---

## 附录 A · 一键复用 Demo 的最小埋点集

若贵方流程与 **玑源长视频 Demo** 一致，**本问卷第三～六节可简答「全部复用 Demo」**，直接开发：

| 场景 | SDK 调用参考 |
|------|--------------|
| 推荐流 | `enterPage(Pages.VIDEO_FEED)` + `bind(video_card)` |
| 进播放页 | `element_click` → `enterPage(Pages.VIDEO_DETAIL)` |
| 播放 | `bizEvent(BizEvents.VIDEO_PLAY_START / HEARTBEAT / END)` |
| 选集 | `enterPage(Pages.VIDEO_SERIES)` + `episode_item` |

参考工程：`examples/android-video-demo`（Android）

---

## 附录 B · 参考文档

| 文档 | 路径 |
|------|------|
| **接入 FAQ** | `docs/tracking/08-接入常见问题FAQ.md` |
| 接入六步 | `docs/tracking/06-接入指南.md` |
| 命名规范 | `docs/tracking/03-命名与登记规范.md` |
| 协议 | `docs/tracking/02-上报协议规范.md` |
| 测试环境发布 | `deploy/DEPLOYMENT.md` |

---

**问卷版本：** v1.0  
**维护：** GISO 数据团队  
**回传方式：** 邮件 / 飞书 / 填好后 Markdown 发回
